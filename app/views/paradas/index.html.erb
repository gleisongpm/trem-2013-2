<h1>Listando paradas</h1>

<table>
  <thead>
    <tr>
      <th>Ordem</th>
      <th>Linha</th>
      <th>Estacao</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @paradas.each do |parada| %>
      <tr>
        <td><%= parada.ordem %></td>
        <td><%= parada.linha.nome %></td>
        <td><%= parada.estacao.nome %></td>
        <td><%= link_to 'Show', parada %></td>
        <td><%= link_to 'Edit', edit_parada_path(parada) %></td>
        <td><%= link_to 'Excluir', parada, method: :delete, data: { confirm: 'Você tem certeza que deseja excluir?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Parada', new_parada_path %>
